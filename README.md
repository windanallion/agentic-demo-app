# Agentic Demo App

AI-generated login page built via the full agentic pipeline:

**Prompt → Design Spec → React Code → GitHub → Linear → Railway → Slack**

## Stack
- React 18 + Vite
- Deployed on Railway

## Run locally
```
npm install
npm run dev
```
